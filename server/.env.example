# Copy this file to `server/.env` and fill in real values (do NOT commit `server/.env`).
# Example environment variables required by the server
OPENAI_API_KEY=REPLACE_WITH_YOUR_OPENAI_KEY

### Optional runtime overrides
# PORT=3000
EOF
